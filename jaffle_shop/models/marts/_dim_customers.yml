models:
  - name: dim_customers
    description: One row represents a customer.
    columns:
      - name: customer_id
        description: This is the primary key.
        data_tests:
          - unique
          - not_null