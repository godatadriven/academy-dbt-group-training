sources:
  - name: jaffle_shop
    description: This is the source for the jaffle_shop app
    schema: jaffle_shop
    tables:
      - name: customers
        description: One row for each order. A customer can make 0 or 1 orders.
        identifier: customers
        columns:
          - name: id
            data_tests:
              - not_null
              - unique
      - name: orders
        identifier: orders
