deploy:
  outputs:
    dev:
      dataset: dbt_lushep
      job_execution_timeout_seconds: 300
      job_retries: 1
      location: EU
      method: oauth
      priority: interactive
      project: sql-training-422508
      threads: 4
      type: bigquery

    prod:
      dataset: dbt_lushep_prod
      job_execution_timeout_seconds: 300
      job_retries: 1
      location: EU
      method: service-account
      keyfile: "{{ env_var('GCP_SERVICE_ACCOUNT') }}"
      priority: interactive
      project: sql-training-422508
      threads: 4
      type: bigquery

  target: dev
