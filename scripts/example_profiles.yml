databricks_dev:
  target: dev
  outputs:
    dev:
      type: databricks
      host: "{{ env_var('DATABRICKS_HOST') }}"
      http_path: "{{ env_var('DATABRICKS_HTTP_PATH') }}"
      schema: "{{ env_var('DBT_SCHEMA', 'dbt_dev') }}"
      catalog: "{{ env_var('DATABRICKS_CATALOG', 'bng_databricks_workspace') }}"
      token: "{{ env_var('DATABRICKS_TOKEN') }}"
      threads: 4
